// stream.service.js
async function getStreamHealth(streamId) {
  // Placeholder: Return stream health metrics
  return { bitrate: 2500, fps: 30, status: "good" };
}

module.exports = { getStreamHealth };
