// viewer.service.js
async function trackViewer(streamId, userId) {
  // Placeholder: Track viewer metrics (e.g., increment viewer count)
  console.log(`Viewer ${userId} is watching stream ${streamId}`);
}

module.exports = { trackViewer };
