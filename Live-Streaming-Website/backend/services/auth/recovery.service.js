// recovery.service.js
async function sendPasswordReset(email) {
  // Placeholder: Generate reset token, send email
  console.log(`Send password reset to ${email}`);
}

module.exports = { sendPasswordReset };
