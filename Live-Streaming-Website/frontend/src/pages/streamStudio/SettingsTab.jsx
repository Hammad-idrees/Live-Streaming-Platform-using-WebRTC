import React from "react";

const SettingsTab = ({
  streamSettings,
  setStreamSettings,
  handleStreamSettingChange,
}) => {
  return (
    <div>
      {/* Settings UI goes here */}
      {/* Use props to build out the form and controls */}
    </div>
  );
};

export default SettingsTab;
